# Deployment requirements (Render/Railway). CPU-only torch keeps the build small
# enough for free tiers — the default CUDA torch wheel is ~2GB and overruns them.
# Local development still uses requirements.txt.
fastapi
uvicorn[standard]
websockets
pandas
numpy
scikit-learn
xgboost
joblib
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.2+cpu
